import Header from './components/Header';
import { Box } from '@chakra-ui/react';

const Landing = () => {
    return (
        <Box px={20} py={8} >
            <Header />
        </Box>
    );
}

export default Landing;
